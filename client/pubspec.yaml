name: client
description: "Miro-like collaborative editing board client"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  hooks_riverpod: ^2.6.1
  flutter_hooks: ^0.21.2
  # Model
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  # WebSocket
  web_socket_channel: ^3.0.2
  # Utilities
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Code Generation
  build_runner: ^2.4.15
  freezed: ^3.0.4
  json_serializable: ^6.9.5
  riverpod_generator: ^2.6.5
  custom_lint: ^0.7.1
  riverpod_lint: ^2.6.5

flutter:
  uses-material-design: true
